services:
  db:
    image: pgvector/pgvector:pg17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: kifrs
      POSTGRES_USER: kifrs
      POSTGRES_PASSWORD: kifrs
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
